[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Single-page app: any route should fall back to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
